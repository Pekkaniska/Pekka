
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	Реквизиты = ПодключаемоеОборудованиеУТВызовСервера.ДанныеЖурналаФискальныхОпераций(Запись.ДокументОснование);
	
	Если Реквизиты = Неопределено Тогда
		
		Элементы.Страницы.ОтображениеСтраниц = ОтображениеСтраницФормы.Нет;
		
	Иначе
		
		ТекстСообщенияXML = Реквизиты.Данные.Получить();
		
		Если ЗначениеЗаполнено(ТекстСообщенияXML) Тогда
			
			ПараметрыФорматирования = ИнтеграцияГосИС.ПараметрыФорматированияXML();
			
			ТекстXML.УстановитьТекст(
				ИнтеграцияГосИС.ФорматироватьXMLСПараметрами(
					ТекстСообщенияXML, ПараметрыФорматирования));
			
			Элементы.Страницы.ОтображениеСтраниц = ОтображениеСтраницФормы.ЗакладкиСверху;
			
		Иначе
			
			Элементы.Страницы.ОтображениеСтраниц = ОтображениеСтраницФормы.Нет;
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры
