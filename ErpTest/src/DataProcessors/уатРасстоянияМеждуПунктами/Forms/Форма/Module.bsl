
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если АвтоТест Тогда
		Возврат;
	КонецЕсли;
	
	Отказ = Истина;
	ПоказатьПредупреждение(Неопределено, "Эта обработка вызывается только для представления контактной информации 
	               |без использования формы обработки.",
	               , 
	               "Представление контактной информации.");
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		АвтоТест = Истина;
		Возврат;
	КонецЕсли;
	
КонецПроцедуры
