
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Счет") Тогда
        Счет = Параметры.Счет;
	Иначе
		Закрыть();
    КонецЕсли;
	
	Комментарий	= Счет.Комментарий;
	Примечание	= Счет.пкПримечание;
	
КонецПроцедуры

&НаКлиенте
Процедура КомандаОк(Команда)
	
	Закрыть(Новый Структура("Счет, Комментарий, Примечание", Счет, Комментарий, Примечание));
	
КонецПроцедуры


