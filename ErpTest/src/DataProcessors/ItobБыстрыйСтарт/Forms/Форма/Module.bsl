
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	МакетСЧегоНачать = Обработки.ItobБыстрыйСтарт.ПолучитьМакет("БыстрыйСтарт").ПолучитьТекст();
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура МакетСЧегоНачатьПриНажатии(Элемент, ДанныеСобытия, СтандартнаяОбработка)
	                                              
	Если Найти(ДанныеСобытия.href, "unf://") <> 0 Тогда
	
		СтандартнаяОбработка = Ложь;
		СтрокаИмяФормы = ДанныеСобытия.href;
		Если Найти(ДанныеСобытия.href, "v8cfgHelp") <> 0 Тогда
			СтрокаИмяФормы = СтрЗаменить(СтрокаИмяФормы, "v8cfgHelp/v8config/", "");	
		КонецЕсли;
		СтрокаИмяФормы = СтрЗаменить(СтрокаИмяФормы, "unf://", "");
		СтрокаИмяФормы = СтрЗаменить(СтрокаИмяФормы, "/", "");
		Попытка
			ОткрытьФорму(СтрокаИмяФормы);
		Исключение
			ItobОбщегоНазначенияСервер.ЗаписьЖурналаРегистрацииОшибка(НСтр("ru = 'Ошибка открытия формы быстрого старта!'", "ru"), ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
		КонецПопытки;	
	
	КонецЕсли;			   
	
КонецПроцедуры

#КонецОбласти
