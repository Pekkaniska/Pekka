
&НаСервере
Процедура СтатусПриИзмененииНаСервере()
    
    Если Объект.Статус = Перечисления.пкСтатусыТехники.НаБазе Тогда 
        Объект.ЗаявкаНаАрендуТехники = Неопределено;
        Объект.ОбъектСтроительства   = Неопределено;
        
        Элементы.ЗаявкаНаАрендуТехники.Видимость = Ложь;
        Элементы.ОбъектСтроительства.Видимость   = Ложь;
        
        Элементы.Склад.Видимость                 = Истина;
    Иначе
        Объект.Склад = Неопределено;
        
        Элементы.Склад.Видимость                 = Ложь;
        Элементы.ЗаявкаНаАрендуТехники.Видимость = Истина;
        Элементы.ОбъектСтроительства.Видимость   = Истина;
    КонецЕсли;    
    
КонецПроцедуры

&НаКлиенте
Процедура СтатусПриИзменении(Элемент)
    
    СтатусПриИзмененииНаСервере();
    
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    
    Объект.Ответственный = Пользователи.ТекущийПользователь();
    
    СтатусПриИзмененииНаСервере();
    
КонецПроцедуры
