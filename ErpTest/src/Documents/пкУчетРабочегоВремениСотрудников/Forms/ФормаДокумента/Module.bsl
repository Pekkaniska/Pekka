
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УстановитьВидимость();
КонецПроцедуры


&НаКлиенте
Процедура УстановитьВидимость()
	
	Для Счетчик = 29 По 31 Цикл 
		Элементы["СписокСотрудниковПланД" + Счетчик].Видимость = Истина; 
		Элементы["СписокСотрудниковФактД" + Счетчик].Видимость = Истина;
	КонецЦикла;   
	
	Если ЗначениеЗаполнено(Объект.Дата) Тогда
		
		КоличествоДней = День(КонецМесяца(Объект.Дата));	
		
		Для Счетчик = КоличествоДней + 1 По 31 Цикл 
			Элементы["СписокСотрудниковПланД" + Счетчик].Видимость = Ложь; 
			Элементы["СписокСотрудниковФактД" + Счетчик].Видимость = Ложь;
		КонецЦикла;                                 	
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДатаПриИзменении(Элемент)
	УстановитьВидимость();
КонецПроцедуры



