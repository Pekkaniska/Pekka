
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ПараметрыФормы = Новый Структура("", );
	Попытка
		мИмяФормы = ИмяФормыПоПараметру(ПараметрКоманды);
		Если мИмяФормы = "" Тогда
			Возврат;
		КонецЕсли;	
		ОткрытьФорму(мИмяФормы, ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно, ПараметрыВыполненияКоманды.НавигационнаяСсылка);
	Исключение
	КонецПопытки;	
КонецПроцедуры


&НаСервере
Функция ИмяФормыПоПараметру (ПараметрКоманды)
	Если ПараметрКоманды.ИмяТипа <> "ТабличныеДанные" Тогда 
		Возврат ПараметрКоманды.ИмяТипа + "." + ПараметрКоманды.ИмяТаблицы + ".ФормаСписка";	
	Иначе
		Возврат "";
	КонецЕсли;
КонецФункции	