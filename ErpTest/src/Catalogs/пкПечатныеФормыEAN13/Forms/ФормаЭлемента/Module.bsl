
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	СписокДоступныхДокументов = ПолучитьСписокДоступныхДокументов();
	Элементы.НаименованиеТипаДокумента.СписокВыбора.Очистить();
	Для Каждого ТекТип Из СписокДоступныхДокументов Цикл
		Элементы.НаименованиеТипаДокумента.СписокВыбора.Добавить(ТекТип, ТекТип);
	КонецЦикла;
	Если ЗначениеЗаполнено(Объект.НаименованиеТипаДокумента) Тогда 
		Объект.НаименованиеТипаДокумента = Объект.НаименованиеТипаДокумента;	
	КонецЕсли;
КонецПроцедуры

&НаСервере
Функция ПолучитьСписокДоступныхДокументов()
	
	МассивВозврат = Новый Массив;
	
	ТипыДокументов = Метаданные.ОпределяемыеТипы.пкТипыДокументовПФEAN13.Тип;
	
	Для Каждого ТекДок Из Метаданные.Документы Цикл
		Если ТипыДокументов.СодержитТип(ТекДок.СтандартныеРеквизиты.Ссылка.Тип.Типы()[0]) Тогда
			МассивВозврат.Добавить(ТекДок.Синоним);
		КонецЕсли;
	КонецЦикла;	
	
	Возврат МассивВозврат;
	
КонецФункции

&НаКлиенте
Процедура НаименованиеТипаДокументаПриИзменении(Элемент)
	// Вставить содержимое обработчика.
КонецПроцедуры
