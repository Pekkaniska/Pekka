
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Список = Новый СписокЗначений;
	Список.Добавить("ARM");
	Список.Добавить("x86");
	Ответ = Вопрос("Выберите вид модель устройства",Список);
	Диалог = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Сохранение);
	Диалог.Фильтр = ".apk|.apk";
	Если Диалог.Выбрать() Тогда
		ДД  = ПолучитьДистрибутив(Ответ);
		ДД.Записать(Диалог.ПолноеИмяФайла);
	КонецЕсли;
КонецПроцедуры

&НаСервере
Функция ПолучитьДистрибутив (Data)
	ИмяФайла = Константы.пкКаталогВременныхФайлов.Получить() + "\UPDATE\" + Константы.пкАктуальнаяВерсияМП.Получить() + "\evgeniy.v.bystrov.torgpred-" + Data + ".apk";
	Файл = Новый Файл(ИмяФайла);
	Если Файл.Существует() Тогда
		Возврат Новый ДвоичныеДанные(ИмяФайла);
	КонецЕсли;		
КонецФункции	
