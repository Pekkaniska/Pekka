
////////////////////////////////////////////////////////////////////////////////
// Интерфейс взаимодействия с общими модулями конфигурации ERP.
//
// Серверные методы.
//  
////////////////////////////////////////////////////////////////////////////////

#Область СлужебныеПроцедурыИФункции

Функция уатГоловнаяОрганизация(Организация) Экспорт
	
	Попытка
		МодульОбщегоНазначенияБПВызовСервераПовтИсп = ОбщегоНазначения.ОбщийМодуль("ОбщегоНазначенияБПВызовСервераПовтИсп");
	Исключение
		МодульОбщегоНазначенияБПВызовСервераПовтИсп = Неопределено;
	КонецПопытки;
	
	Если МодульОбщегоНазначенияБПВызовСервераПовтИсп = Неопределено Тогда
		Возврат Организация;
	Иначе 
		Возврат МодульОбщегоНазначенияБПВызовСервераПовтИсп.ГоловнаяОрганизация(Организация);
	КонецЕсли;
	
КонецФункции 

Функция уатЗначениеФункциональнойОпции(Имя) Экспорт
	
	Если Метаданные.ФункциональныеОпции.Найти(Имя) = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Попытка
		МодульОтчетыУТВызовСервераПереопределяемый = ОбщегоНазначения.ОбщийМодуль("ОтчетыУТВызовСервераПереопределяемый");
	Исключение
		МодульОтчетыУТВызовСервераПереопределяемый = Неопределено;
	КонецПопытки;
	
	Если МодульОтчетыУТВызовСервераПереопределяемый = Неопределено Тогда
		Возврат ПолучитьФункциональнуюОпцию(Имя);
	Иначе 
		Возврат МодульОтчетыУТВызовСервераПереопределяемый.ЗначениеФункциональнойОпции(Имя);
	КонецЕсли;
	
КонецФункции

#КонецОбласти
